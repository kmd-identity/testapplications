<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KMD.Identity.TestApplications.OpenID.MAUI.MainPage"
             xmlns:Converters="clr-namespace:KMD.Identity.TestApplications.OpenID.MAUI.Converters"
             >
    <ScrollView>
        <StackLayout>
            <VerticalStackLayout
                Spacing="25"
                Padding="30,0"
                VerticalOptions="Center"
                IsVisible="{Binding IsAuthenticated, Converter={Converters:InverseBoolConverter}}">

                <Label
                    Text="You are currently not logged-in."
                    SemanticProperties.HeadingLevel="Level1"
                    FontSize="32"
                    HorizontalOptions="Center" />

                <Button
                    x:Name="LoginBtn"
                    Text="Click me"
                    SemanticProperties.Hint="Click here to log-in"
                    Clicked="OnLoginStart"
                    HorizontalOptions="Center" />

            </VerticalStackLayout>
            <VerticalStackLayout
                Spacing="25"
                Padding="30,0"
                VerticalOptions="Center"
                IsVisible="{Binding IsAuthenticated}">

                <Label
                    Text="You have logged in successfully. Your claims are:"
                    SemanticProperties.HeadingLevel="Level1"
                    FontSize="32"
                    HorizontalOptions="Center" />

                <Label
                    Text="{Binding Claims}">
                </Label>

            </VerticalStackLayout>
        </StackLayout>

    </ScrollView>

</ContentPage>
